[pytest]
addopts = --html=reports/report.html --self-contained-html -v -s 
testpaths = tests
log_cli = true
log_cli_level = INFO
markers =
    table: test untuk validasi data tabel lokal
    wait: marks tests that use wait logic for UI elements
    navigation: marks tests related to navigation actions
    dynamic: test untuk menunggu elemen muncul setelah proses loading
    waitbutton: test untuk menunggu tombol aktif (latihan 14)waitelement: test menunggu elemen muncul
    waitbutton: test menunggu tombol aktif
    waitelement: test menunggu elemen muncul
    waitdisappear: test menunggu elemen hilang
    waitnavigation: test menunggu navigasi halaman
    waitresponse: test menunggu respon API
    waitrequest: test menunggu request API dikirim
    dialog: test handling browser dialogs (alert, confirm, prompt)
    multipletabs: test handling multiple tabs / windows
    iframe: test handling iFrame
    keyboardmouse: test keyboard & mouse actions
    assertions: tests for advanced assertions
    keyboardmouse: tests for keyboard and mouse actions
    assertions: test assert lanjutan
    network: test untuk intercept atau mock response API
    networklog: test untuk memantau dan mencatat network request/response
    multipletabs: test handling multiple browser tabs
    iframe: test iframe handling
    keyboardmouse: test keyboard and mouse actions
    assertions: test assertions advanced
    network: test network intercepts
    networklog: test network logging
    networklogjson: test detailed network logging to JSON
    waitbutton: test tunggu tombol aktif
    dialog: test dialog box
    multipletabs: test multi tab
    iframe: test iframe
    keyboardmouse: test interaksi keyboard & mouse
    assertions: test assert lanjutan
    network: test intercept request
    networklog: test log jaringan
    networklogjson: test log jaringan format json
    visual: test visual regression
    fileupload: test upload file
    filedownload: test download file
    video: mark tests that record video on failure
    smoke: untuk test ringan yang cepat
    regression: untuk test fitur lama yang perlu dicek ulang
    slow: untuk test yang memakan waktu lama